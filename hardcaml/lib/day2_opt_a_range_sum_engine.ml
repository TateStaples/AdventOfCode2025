(* Day 2 — Paper Option A: Range-Sum Engine (Area-Optimized)

   This is a thin wrapper around the existing sequential family-iterator
   implementation, which matches the paper’s “loop over families, loop over
   ranges” micro-architecture.
*)

include Day2_opt_a_sequential
